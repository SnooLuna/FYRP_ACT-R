[eq](https://www.lispworks.com/documentation/HyperSpec/Body/f_eq.htm#eq) is a [[function vs macro|function]] that accepts two arguments. It returns [[t]] if these two arguments are the same object in memory, otherwise it returns [[nil]]. 

used by and not the same as [[equal]] or [[equalp]].